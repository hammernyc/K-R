#include <stdio.h>

int main(int argc, char *argv[])
{
    int i, c;
    const int lim = 100;
  //  for (i = 0; i < lim - 1 && (c = getchar()) != '\n' && c != EOF; i++) ;

  while (i < lim - 1) {
    if ((c = getchar()) != '\n') {
        if (c != EOF) {
          ;  
        }
    }
  }
}
